import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.gcs08.reddonacion',
  appName: 'RedDonación',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
